[build-system]
requires = ['setuptools',
			'scipy','matplotlib','tqdm','numpy','ipython','joblib',
			'celluloid','psutil','gdown','pandas','plotly',
			'ipywidgets','nbformat']
build-backend = "setuptools.build_meta"